# Basic dependabot.yml file with
# minimum configuration for one package managers

version: 2
updates:
  # Enable version updates for maven
  - package-ecosystem: "maven"
    # Look for `pom.xml` in the `root` directory
    directory: "/"
    # Check the maven registry for updates on monday
    schedule:
      interval: "weekly"
